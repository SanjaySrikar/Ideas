import { SortTopicPipe } from './sort-topic.pipe';

describe('SortTopicPipe', () => {
  it('create an instance', () => {
    const pipe = new SortTopicPipe();
    expect(pipe).toBeTruthy();
  });
});
